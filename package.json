{
  "name": "serverless-plugin-iam-checker",
  "description": "A serverless plugin to check IAM policies for security compliance",
  "version": "0.0.45",
  "scripts": {
    "publish-please": "publish-please",
    "prepublishOnly": "rimraf dist/ && npm run build && publish-please guard",
    "build": "tsc -p ./tsconfig.build.json",
    "test": "SLS_DEBUG=* nyc --exclude=src/mock-data/** --exclude=src/*.cucumber* node_modules/cucumber/bin/cucumber-js src/*.feature",
    "pretest": "tsc -p ./tsconfig.json",
    "posttest": "npm run reportcoverage && npm run clean",
    "reportcoverage": "nyc report --reporter=json --reporter=lcov",
    "clean": "rimraf src/**/*.js* src/*.js* src/**/**/*.js* src/**/*.d.ts src/*.d.ts src/**/**/*.d.ts",
    "codecov": "codecov -f coverage/*.json",
    "packagesampleapp": "cd examples && npm i && sls package"
  },
  "devDependencies": {
    "@types/node": "^12.6.2",
    "chai": "^4.2.0",
    "codecov": "^3.5.0",
    "cucumber": "^5.1.0",
    "cucumber-tsflow": "^3.2.0",
    "nyc": "^14.1.1",
    "publish-please": "^5.5.0",
    "rimraf": "^2.6.3",
    "typescript": "^3.5.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/manwaring/serverless-plugin-iam-checker.git"
  },
  "bugs": {
    "url": "https://github.com/manwaring/serverless-plugin-iam-checker"
  },
  "homepage": "https://github.com/manwaring/serverless-plugin-iam-checker",
  "main": "dist/plugin.js",
  "types": "dist/plugin.d.ts",
  "author": "From Phillip with ♥",
  "license": "MIT",
  "keywords": [
    "serverless",
    "plugin",
    "iam",
    "security"
  ]
}
